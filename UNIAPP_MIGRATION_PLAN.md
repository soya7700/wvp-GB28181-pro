# WVP Web 管理后台迁移到 uni-app + Vue 3 计划

## 1. 目标与原则

最终目标是新增一套基于 **uni-app + Vue 3** 的移动端管理应用，可发布为：

- 移动端 H5（第一阶段，作为主要调试目标）
- Android App
- iOS App
- 微信小程序

原有 `web` 管理后台继续保留，新的移动端项目建议放在仓库根目录的 `uniapp/`，不在旧项目上直接升级。这样可以避免影响现有桌面后台，并允许两套前端共用同一套 WVP 后端 API。

## 2. 现状与主要迁移难点

现有 `web` 是 Vue 2.6 + Element UI + Vue Router 3 + Vuex 3 + Axios 的桌面管理后台，包含设备、通道、直播、录像、推拉流、级联、组织结构、用户和运维等模块。

需要重点处理的差异：

1. **UI 不能直接复用**：`el-table`、`el-dialog`、树形控件、桌面侧栏和右键菜单需要改造成移动端列表、卡片、抽屉、分步表单和选择页。
2. **路由机制不同**：Vue Router 路由需转换为 uni-app 的 `pages.json`、分包和页面跳转 API。
3. **状态管理升级**：Vuex 模块建议按业务域迁移到 Pinia，并只保留跨页面状态；普通列表数据留在页面或 composable 内。
4. **请求层适配**：Axios 与 Element UI 消息提示需替换为基于 `uni.request` 的统一请求层、Token 注入、401 重登和统一错误提示。
5. **播放器跨端差异大**：当前 Jessibuca、H265 WebAssembly、ZLM WebRTC、WS-FLV 都明显依赖浏览器 DOM。H5 可优先复用或封装；App 与小程序必须分别评估 `<video>`、原生播放器、HLS/WebRTC 支持以及流协议转换。
6. **浏览器 API 不可直接跨端**：`window`、`document`、Blob 下载、全屏、剪贴板、WebSocket、文件上传、OpenLayers 地图都要封装成平台适配层。
7. **桌面交互不适合手机**：分屏监控、大表格、复杂树、录像时间轴和多字段编辑表单需要重新设计，不建议机械缩小。

## 3. 建议的首期 H5 范围（MVP）

首期以“手机上可完成日常查看与基础控制”为目标：

### P0：必须完成

- 登录、退出、登录过期处理
- 首页概览（设备、通道、推流、代理等核心统计）
- 国标设备列表、搜索、在线状态、设备详情
- 通道列表、搜索、分页、在线状态
- 单路实时预览
- 基础云台控制（方向、停止、变倍；以后端现有能力为准）
- 推流列表与拉流代理列表的查看、启停
- 基础个人设置/修改密码

### P1：H5 稳定后补齐

- 云端录像列表、录像详情和回放
- 国标录像查询、时间轴和下载任务
- 组织结构（行政区划、业务分组）浏览与选择
- 媒体节点状态
- 告警/运行日志的移动端查看
- 设备、代理、推流、级联等常用编辑操作

### P2：后续管理能力

- 多画面分屏（移动端建议上限 4 画面）
- 录制计划编辑
- 用户、API Key、级联平台等高风险管理功能
- 导入导出、日志文件下载、复杂批量操作
- 地图能力与通道点位展示

## 4. 技术方案

建议技术基线：

- uni-app + Vue 3 + TypeScript + Vite
- Pinia
- uni-ui 或兼容多端的 Vue 3 移动组件库；避免依赖仅支持 H5 的组件
- `uni.request` 封装请求客户端
- SCSS + `rpx`，并设定手机/平板最大内容宽度
- ESLint + TypeScript 检查

建议目录：

```text
uniapp/
├── src/
│   ├── api/              # 按 device、play、record 等领域拆分
│   ├── components/       # 跨页面移动组件
│   ├── composables/      # 播放、分页、权限等组合逻辑
│   ├── pages/            # 主包页面
│   ├── pages-device/     # 设备相关分包
│   ├── pages-record/     # 录像相关分包
│   ├── pages-admin/      # 管理功能分包
│   ├── platform/         # H5/App/小程序平台适配
│   ├── stores/           # Pinia
│   ├── styles/
│   ├── types/
│   └── utils/
├── pages.json
├── manifest.json
└── vite.config.ts
```

请求层应统一做到：API 基地址配置、`access-token` 注入、超时、业务 `code` 判断、401 清理登录态、错误标准化、上传/下载独立封装。不要把 UI 提示写进底层 API 文件，由页面决定 toast 或弹窗方式。

## 5. 分阶段实施

### 阶段 0：接口与能力盘点（2～4 人日）

- 为现有 `src/api` 中的接口生成清单：路径、方法、参数、响应、使用页面。
- 核对后端跨域、HTTPS、WebSocket、媒体流地址和 Token 规则。
- 用真机浏览器验证 HLS、WS-FLV、WebRTC、H.264/H.265 的实际可播放组合。
- 确定首期播放器路线和不支持时的降级提示。

交付物：接口矩阵、页面矩阵、协议兼容性报告、MVP 冻结清单。

### 阶段 1：工程骨架（2～3 人日）

- 创建独立 `uniapp/` Vue 3 + TypeScript 项目。
- 配置开发/测试/生产环境和 H5 代理。
- 建立请求层、Pinia 登录态、权限守卫、主题、基础布局和分包。
- 建立组件与代码规范，并接入类型检查和构建检查。

验收：H5 可启动、可连接后端、可登录退出、刷新后恢复登录态、401 能正确回到登录页。

### 阶段 2：P0 移动端 H5（10～15 人日）

- 实现登录、首页、设备、通道、推流、代理、个人设置。
- 将桌面表格统一改为卡片列表 + 筛选抽屉 + 下拉刷新/触底分页。
- 实现单路预览与基础云台控制。
- 对加载、空数据、断网、接口异常、流播放失败提供统一反馈。

验收：主流 Android/iOS 手机浏览器中完成 P0 主流程；窄屏无横向溢出；弱网和 Token 失效可恢复。

### 阶段 3：录像和进阶功能（8～12 人日）

- 迁移云端录像、国标录像、回放和移动版时间轴。
- 迁移组织结构、媒体节点、日志等 P1 页面。
- 将复杂编辑表单拆成分组或分步流程。
- 实现 H5 文件下载、截图和全屏的能力封装。

验收：回放、检索、停止播放、页面离开时释放流等关键生命周期正确。

### 阶段 4：Android/iOS 适配（5～10 人日，不含原生播放器研发）

- 处理安全域名、证书、权限、后台切换、屏幕方向和文件存储。
- 使用条件编译实现 App 播放、下载、分享和全屏适配。
- 若 H5 播放器在 WebView 中性能不足，选用 uni-app 原生插件或开发原生播放器桥接。
- 完成真机稳定性、功耗、内存和长时间播放测试。

### 阶段 5：微信小程序适配（5～10 人日，取决于流协议）

- 配置 request/socket/upload/download 合法域名和 HTTPS/WSS。
- 替换 H5 专属播放器、地图、下载和 DOM 操作。
- 根据账号资质和微信支持能力选用 `<video>` 或 `<live-player>`。
- 控制主包体积并细化分包；完成微信真机和审核规范检查。

注意：小程序直播能力、可用协议和类目资质是独立风险项，不能假设 H5 播放方案可直接运行。

## 6. 页面迁移映射

| 原后台模块 | 移动端形态 | 优先级 |
| --- | --- | --- |
| 登录 | 独立登录页 | P0 |
| 控制台 | 统计卡片、健康状态、快捷入口 | P0 |
| 国标设备 | 卡片列表、筛选页、详情页 | P0 |
| 设备通道 | 卡片列表、预览页、控制面板 | P0 |
| 分屏监控 | 单路优先，后续 2/4 宫格 | P0/P2 |
| 推流/拉流代理 | 列表、详情、启停；编辑后置 | P0/P1 |
| 云端/国标录像 | 日期筛选、录像列表、回放页 | P1 |
| 行政区划/业务分组 | 独立树选择页，按层级懒加载 | P1 |
| 媒体节点 | 状态卡片与详情 | P1 |
| 运维日志 | 筛选列表/流式日志，下载后置 | P1/P2 |
| 录制计划 | 周计划编辑器，单独重做触摸交互 | P2 |
| 用户/API Key/级联 | 独立管理页并增加二次确认 | P2 |

## 7. 播放能力专项建议

播放器应设计为统一业务组件 `MediaPlayer`，输入流信息而不是固定 URL，由平台策略选择实现：

- **H5 首选**：优先验证 HLS（兼容性较好）和 WebRTC（低延迟）；WS-FLV/Jessibuca 作为特定浏览器降级方案。
- **Android/iOS App**：先测试系统 `<video>`；不满足 H.265、低延迟或稳定性要求时切换原生播放器插件。
- **微信小程序**：优先使用微信支持的直播/视频组件，并由媒体服务提供其支持的协议；不移植 Jessibuca DOM/WASM 实现。
- 页面隐藏、切后台、路由离开时必须停止播放并释放解码器、Socket 和后端拉流会话。

## 8. 测试与验收基线

- 单元测试：请求层、Token、数据转换、关键 store/composable。
- 接口测试：登录、分页、播放/停止、录像、PTZ、启停操作。
- H5 设备矩阵：Android Chrome/微信内置浏览器、iOS Safari/微信内置浏览器。
- 视觉检查：375px、390px、430px 和平板宽度。
- 性能：首屏、列表滚动、长时间播放、播放器反复进入退出后的内存。
- 安全：HTTPS/WSS、Token 泄露检查、敏感操作二次确认、日志脱敏。

## 9. 推荐的第一个开发迭代

第一个迭代只做一条完整纵向链路：

1. 创建 `uniapp/` 工程和环境配置。
2. 完成统一请求层、登录态和登录页。
3. 完成设备列表 → 通道列表 → 单路播放 → 停止播放。
4. 在 Android Chrome 和 iOS Safari 真机验证。
5. 根据流兼容结果决定后续播放器技术路线，再扩展其余页面。

这条链路能最早暴露项目最大的技术风险（鉴权、网络、流协议和移动端解码），适合作为是否全面迁移的首个里程碑。
